import React from "react";

const HEADER_TITLE = '헤더 타이틀'

function Header() {
    return (
        <div id="header">
            <strong>{HEADER_TITLE}</strong>
        </div>
    )
}

export default Header;