import React from "react";

const ROOT_TITLE = '메인 페이지'

function ROOT() {
    return (
        <div id="header">
            <strong>{ROOT_TITLE}</strong>
        </div>
    )
}

export default ROOT;